import NavBar from "../navBar/NavBar";

const Header = () => {
	return (
		<div className='fixed z-30 flex flex-row justify-center items-center w-full '>
			<NavBar />
		</div>
	);
};

export default Header;
